# 2.9.4

- Use the platform's supported Critical priority for estimation and sprint-preflight commitments in both planning and attention recovery.
- Fix the schema rejection that stopped a successfully reconciled backlog before role estimation could begin. Existing accepted proposals and ticket identities are retained.
